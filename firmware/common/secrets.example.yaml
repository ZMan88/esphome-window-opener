# Copy to secrets.yaml (git-ignored) and fill in real values.
#
# Generate the API encryption key with:
#   openssl rand -base64 32

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_password: "CHANGE_ME_FALLBACK_AP_PASSWORD"
api_encryption_key: "GENERATE_32_BYTE_BASE64_KEY"
ota_password: "CHANGE_ME_OTA_PASSWORD"
